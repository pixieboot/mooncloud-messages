<nav class="navbar navbar-expand-lg fw-bold bg-light">
  <div class="container">
    <hr />
    <div class="logo">
      <a class="navbar-brand" href="/">
        <img
          src="assets/logo/mooncloud_logo_128x128.png"
          alt="Logo"
          width="34"
          height="34"
          class="d-inline-block mb-1"
        />
        <strong>Mooncloud Messages</strong>
      </a>
    </div>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <% if(typeof user != 'undefined') { %>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav text-center">
        <li
          class="nav-item d-flex align-items-center justify-content-center my-2 my-lg-0 me-lg-2"
        >
          <a logged-user class="mt-1 text-decoration-none text-dark" href="/user/<%= user.username %>">
            <% if(user.avatar != "" ) { %>
            <img id="user_avatar" class="me-1" src="assets/users/uploads/<%= user.avatar %>" alt="user_avatar" />
            <% } else { %>
            <img id="user_avatar" class="me-1" src="assets/users/default/<%= user.defaultAvatar %>" alt="user_avatar" />
            <% } %>
            <strong><%= user.username %></strong>
          </a>
        </li>
        <li
          class="nav-item d-flex align-items-center justify-content-center my-2 my-lg-0 me-lg-2"
        >
          <form action="/logout?_method=DELETE" method="post">
            <button type="submit" class="btn text-dark mx-auto">
              <strong>Logout</strong>
              <i class="fa-solid fa-right-from-bracket ms-1"></i>
            </button>
          </form>
        </li>
      </ul>
    </div>
    <% } else { %>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav text-center">
        <li
          class="nav-item d-flex align-items-center justify-content-center my-2 my-lg-0 me-lg-2"
        >
          <a
            type="button"
            class="text-decoration-none text-dark"
            data-bs-target="#login-modal"
            data-bs-toggle="modal"
            ><strong>Login</strong></a
          >
        </li>
        <li class="nav-item my-2 my-lg-0 ms-lg-2">
          <button
            type="button"
            class="btn btn-secondary mx-auto"
            data-bs-target="#signup-modal"
            data-bs-toggle="modal"
          >
            <strong>Sign up</strong>
          </button>
        </li>
      </ul>
    </div>
    <% } %>
  </div>
</nav>
